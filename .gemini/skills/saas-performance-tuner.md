# Skill: saas-performance-tuner
## Goal: Sub-second page loads.
## Instructions:
- Optimize Prisma queries to avoid **N+1 problems** using `include` or `select`.
- Implement **Optimistic UI** updates for status changes using `useOptimistic`.
- Use Next.js **Streaming (Suspense)** for all data-heavy tables.
