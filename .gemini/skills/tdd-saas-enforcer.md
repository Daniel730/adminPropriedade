# Skill: tdd-saas-enforcer
## Goal: Fulfill the "Always TDD" mandate for SaaS business logic.
## Instructions:
- For every new feature: Create a failing unit test for the `lib/` utility or `api/` route before writing implementation.
- Use `msw` (Mock Service Worker) for API integration tests.
- Ensure 100% coverage on "Permission Guard" functions.
